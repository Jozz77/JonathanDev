

export default function Button({text}) {
  return (
    <div>
        <button className="border-solid border-black px-4 mt-4 font-semibold py-1 text-[0.85rem] border-[3px] hover:text-gray hover:border-gray">{text} </button>
    </div>
  )
}
